name: rondobyte_landing
description: Landing page oficial da RondoByte.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_svg: ^2.0.10
  url_launcher: ^6.2.5
  google_fonts: ^6.2.1
  file_picker: ^8.1.4

dev_dependencies:
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
